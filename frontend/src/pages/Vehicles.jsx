// Vehicles.jsx — owner: Person A (Fleet & Assets)
// Not built yet — backend endpoints already available:
// GET /vehicles, POST /vehicles, PATCH /vehicles/:id/status
// See routes/vehicles.js for what's ready to wire up.

export default function Vehicles() {
  return (
    <div>
      <h2>Fleet</h2>
      <p style={{ color: 'var(--text-muted)', fontSize: 13 }}>
        Not built yet — suggested owner: <strong>Person A (Fleet & Assets)</strong>.
      </p>
    </div>
  );
}
